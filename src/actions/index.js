import { selectRover, storePhotos, setDateFilter } from './roverActions';
import { startLoading, stopLoading, toggleSidebar } from './uiActions';

export const actionCreators = {
  selectRover,
  storePhotos,
  setDateFilter,
  startLoading,
  stopLoading,
  toggleSidebar
};
